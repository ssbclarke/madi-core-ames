import assert from 'assert'
import rewire from 'rewire'

describe('\'isAllowedByRobotsTxt\' function', () => {
  it('error with null url', () => {
    assert.ok(false);
  });
  it('error with null url', () => {
    assert.ok(false);
  });
  it('standardize the url', ()=> {
    assert.ok(false);
  });
  it('gets root domain',()=>{
    assert.ok(false);
  });
  it('stores locally',()=>{
    assert.ok(false);
  });
  it('fetches locally when previously stored',()=>{
    assert.ok(false);
  });
  it('returns null if not there',()=>{
    assert.ok(false);
  });
  it('stores null if not there',()=>{
    assert.ok(false);
  });
});