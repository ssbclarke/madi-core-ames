import { getCurrentWeather, getCurrentWeatherDesc } from "./getCurrentWeather"

export const availableTools = {
    get_current_weather: getCurrentWeather,
}

export const availableToolDescriptions = [
    getCurrentWeatherDesc
]